# G2.7 真人决策交接说明

核查时间：2026-09-05 19:29:17（北京时间）
当前状态：技术恢复已验证，生产闭环尚未收口。

## 需要真人决定的事项

现有 Director v2 需要真实评测，并由真人决定是否部署。
批准 GitHub 外部测试，不等于该智能体已通过评测，也不替代部署决定。

- 工作空间（Workspace）：`86679997-3f3a-4477-a2fa-d4da812140ae`
- 候选智能体版本（AgentVersion）：`2fd9e700-af69-4df5-b659-2dac15dbed96`（v2）
- 运行时／模型：`codex_local` / `gpt-5.6-sol`
- 内容哈希：`4bea10e869ed227346f464a5e149f778bd8e08cfbd1bd9a024a2a80b6501917b`
- 候选版本的提示词与兼容执行器的能力提示词一致。
- 评测记录（EvaluationRun）：无。尚无可认定的质量、安全、成本或延迟评测结果。
- 执行器的工作目录（cwd）未设置；仍需验证仓库与执行环境的绑定，才能满足 G2 的代码产物执行验收标准。

操作入口：[智能体页面](http://127.0.0.1:3270/MYW/agents)。
没有真实评测依据时，不要将结果记录为 `passed`。候选版本获准后，通过智能体生命周期界面部署，
再通过目标工作台创建绑定新版本的工作图修订（GraphRevision）与运行（Run）。
保留旧 Run 和不可变版本，不原地修改历史事实。

## 已验证的恢复结果

通过带审计的界面恢复命令，真实失败的 outbox 事件及其三个后续事件均已投递。
恢复过程将第 3 次尝试的租约收敛为过期并记录审计，没有自行分配新执行。
随后一次显式界面重试创建了第 4 次尝试；真实主机执行器领取后，将事件游标推进到 2，
因模型不匹配而拒绝执行，并释放租约。该 Run 的全部 10 条 outbox 事件均已投递。
整个过程没有通过直接修改数据库来修复状态。

被拒绝的 Run 绑定 v1 `89eb5235-a873-4034-8fd9-f0b8309edbc0`，其模型为 `unconfigured`。
反复点击“重试”无法使它与配置为 `gpt-5.6-sol` 的执行器兼容。
此次尝试没有产生 Codex 心跳运行记录或产物。

[运行事实页面](http://127.0.0.1:3270/MYW/targets/44cc7194-d8ce-46d3-8626-3da36785eced/runs)
及 `runs/T026-A002/evidence.json` 保留了确切的对象标识与审计记录。

## 后续顺序

1. 完成候选版本的真实评测和真人部署决定，为新执行绑定仓库、环境及选定的不可变版本。
2. 完成真实 Codex 执行并生成按内容寻址的代码产物，固定源代码提交，补齐质量检查与独立验证证据。
3. 由智能体或服务发起交付提交（Submission）和动作请求（ActionRequest），分别取得真人评审（Review）
   与动作审批（ActionApproval），再执行已授权的单次 GitHub 测试 PR 创建操作。
4. 取得外部服务的效果回执（EffectReceipt）及对账证据，再由真人结果负责人（Outcome Owner）
   完成验收（Acceptance）。合并、发布及生成交付发布记录（ship record）均未获授权。

## 验证状态

专项检查已通过：包含真实 Temporal 重放与隔离数据库的 Go 测试、Go 构建、22 项工作台测试、
17 项执行器／路由测试、UI 类型检查与构建，以及设计令牌检查。

全量 Vitest 尚未全部通过：最近一次运行出现 CLI 测试超时，且部分测试组尚未执行。
本交接不表示已经达到 PR 就绪或发布就绪状态。
